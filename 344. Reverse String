class Solution(object):
    def isPalindrome(self, s):
        s1= s.lower()
        s2= " ".join(i for i in s1 if not i.isalnum())
        if(s1==s2[::-1]):
            return True
        else:
            return False     
